<style>
  body {
    text-align: center;
    padding: 20px;
    background-color: #f8f9fa;
  }

  h1, h2 {
    margin-bottom: 10px;
  }

  p {
    margin: 5px 0;
  }

  .table td, .table th {
    font-size: 1.2rem;
  }

  .table input[type="checkbox"] {
    transform: scale(1.5); 
  }

  .table input[type="datetime-local"] {
    font-size: 1rem;
    padding: 5px;
    margin-left: 10px;
  }

  #form-teams-btn {
    width: 100%;
  }

  .selected {
    background-color: #c3e6cb !important;
  }

  .table th {
    background-color: #007bff;
    color: white;
  }

  .table {
    background-color: white;
  }

  #confirmed-players-count {
    font-weight: bold;
    font-size: 1.5rem;
  }

  .hidden {
    display: none;
  }
</style>

<div class="container mt-5">
  <h1 class="mb-4">Futebol - Quarta</h1>

  <p>Selecione os jogadores desejados. Após selecionar, clique em "Formar Times".</p>
  <p>Selecione <strong>pelo menos 12 jogadores</strong> para formar times.</p>
  <p><strong><span id="confirmed-players-count">0</span></strong> jogador(es) confirmado(s).</p>

  <%= form_tag form_teams_players_path, method: :post do %>
    <div class="mb-3">
      <div class="d-grid gap-2">
        <button type="submit" class="btn btn-lg btn-primary" id="form-teams-btn" disabled>Formar Times</button>
      </div>
    </div>
    <div class="table-responsive">
      <table class="table table-bordered text-center">
        <thead>
          <tr>
            <th>Jogador</th>
            <th>Score</th>
            <th>Selecionar</th>
            <th>Data e Hora</th>
          </tr>
        </thead>
        <tbody>
          <% @players.each do |player| %>
            <tr class="table-row">
              <td><%= player.name %></td>
              <td><%= player.score %></td>
              <td><%= check_box_tag 'player_ids[]', player.id %></td>
              <td>
                <%= datetime_field_tag "player_times[#{player.id}]", Time.now, class: 'hidden', disabled: true %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% end %>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    var formTeamsBtn = document.getElementById('form-teams-btn');
    var checkboxes = document.querySelectorAll('input[type="checkbox"]');
    var confirmedPlayersCountElement = document.getElementById('confirmed-players-count');

    checkboxes.forEach(function(checkbox) {
      checkbox.addEventListener('change', function() {
        var selectedPlayers = document.querySelectorAll('input[type="checkbox"]:checked').length;

        // Atualizar a contagem de jogadores confirmados
        confirmedPlayersCountElement.innerText = selectedPlayers;

        // Habilitar/desabilitar o botão de formar times
        formTeamsBtn.disabled = selectedPlayers < 12;

        // Adiciona ou remove a classe 'selected' com base no estado do checkbox
        var row = this.closest('.table-row');
        row.classList.toggle('selected', this.checked);

        // Exibe ou oculta o campo de data/hora
        var datetimeField = row.querySelector('input[type="datetime-local"]');
        if (this.checked) {
          datetimeField.classList.remove('hidden'); // Exibe o campo de data/hora
          datetimeField.value = new Date().toISOString().slice(0, 16); // Define o valor atual
        } else {
          datetimeField.classList.add('hidden'); // Oculta o campo de data/hora
          datetimeField.value = ''; // Limpa o valor ao desmarcar
        }
      });
    });

    // Adiciona evento de clique ao botão para verificar novamente antes do envio do formulário
    formTeamsBtn.addEventListener('click', function() {
      var selectedPlayers = document.querySelectorAll('input[type="checkbox"]:checked').length;
      if (selectedPlayers < 12) {
        alert('Selecione pelo menos 12 jogadores para formar times.');
        return false; // Impede o envio do formulário
      }
    });
  });
</script>
